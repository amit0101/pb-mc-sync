# Pabau-Mailchimp Sync Configuration
# Copy this file to .env and fill in your values

# ==========================================
# PABAU API CONFIGURATION
# ==========================================
# Get these from: Pabau Settings → API
PABAU_API_KEY=your_pabau_api_key_here
PABAU_API_URL=https://api.pabau.com/v2
PABAU_COMPANY_ID=your_company_id_here

# ==========================================
# MAILCHIMP API CONFIGURATION
# ==========================================
# Get these from: Mailchimp Account → Extras → API Keys
MAILCHIMP_API_KEY=your_api_key_here-us19
MAILCHIMP_SERVER_PREFIX=us19
# Get this from: Audience → Settings → Audience name and defaults
MAILCHIMP_LIST_ID=your_list_id_here

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
# For local development (PostgreSQL):
DATABASE_URL=postgresql://user:password@localhost:5432/pb_mc_sync
# For Render (this will be automatically set):
# DATABASE_URL=<provided_by_render>

# ==========================================
# REDIS CONFIGURATION
# ==========================================
# For local development:
REDIS_URL=redis://localhost:6379/0
# For Render (this will be automatically set):
# REDIS_URL=<provided_by_render>

# ==========================================
# APPLICATION SETTINGS
# ==========================================
APP_ENV=development
LOG_LEVEL=INFO

# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=generate_a_random_secret_string_here

# ==========================================
# SYNC SETTINGS (Optional)
# ==========================================
# How many records to fetch per API request
SYNC_BATCH_SIZE=100

# How often to sync recent changes (in seconds)
# 300 = 5 minutes (default)
SYNC_INTERVAL_SECONDS=300

# What hour (UTC) to run nightly reconciliation
# 2 = 2 AM UTC (default)
RECONCILIATION_HOUR=2

# ==========================================
# MONITORING (Optional)
# ==========================================
# Sentry DSN for error tracking (leave empty to disable)
SENTRY_DSN=

# ==========================================
# NOTES
# ==========================================
# 1. Never commit .env file to git (it's in .gitignore)
# 2. For production, set these in Render dashboard
# 3. Generate webhook secret: openssl rand -hex 32
# 4. Mailchimp server prefix is in your API key (part after dash)
# 5. Database and Redis URLs will be auto-set by Render

